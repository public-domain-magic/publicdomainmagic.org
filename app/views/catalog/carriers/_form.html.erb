<%= form_with(model: catalog_carrier) do |form| %>
  <% if catalog_carrier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(catalog_carrier.errors.count, "error") %> prohibited this catalog_carrier from being saved:</h2>

      <ul>
        <% catalog_carrier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
