<%= form_with(model: catalog_medium) do |form| %>
  <% if catalog_medium.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(catalog_medium.errors.count, "error") %> prohibited this catalog_medium from being saved:</h2>

      <ul>
        <% catalog_medium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
