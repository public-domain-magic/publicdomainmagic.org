<%= form_with(model: catalog_series) do |form| %>
  <% if catalog_series.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(catalog_series.errors.count, "error") %> prohibited this catalog_series from being saved:</h2>

      <ul>
        <% catalog_series.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
