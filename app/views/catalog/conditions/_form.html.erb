<%#
SPDX-FileCopyrightText: 2026 Kerrick Design, LLC <me@kerricklong.com>
SPDX-License-Identifier: LicenseRef-LICENSE
-%>
<%= form_with(model: catalog_condition) do |form| %>
  <% if catalog_condition.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(catalog_condition.errors.count, "error") %> prohibited this catalog_condition from being saved:</h2>

      <ul>
        <% catalog_condition.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
