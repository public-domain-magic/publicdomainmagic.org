<%= form_with(model: catalog_intended_audience) do |form| %>
  <% if catalog_intended_audience.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(catalog_intended_audience.errors.count, "error") %> prohibited this catalog_intended_audience from being saved:</h2>

      <ul>
        <% catalog_intended_audience.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
