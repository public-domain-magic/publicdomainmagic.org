<%= form_with(model: catalog_language) do |form| %>
  <% if catalog_language.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(catalog_language.errors.count, "error") %> prohibited this catalog_language from being saved:</h2>

      <ul>
        <% catalog_language.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :code, style: "display: block" %>
    <%= form.text_field :code %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
